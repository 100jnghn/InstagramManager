<Page
    x:Class="InstagramManager.Views.Pages.DataPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:InstagramManager.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:InstagramManager.Models"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:dg="clr-namespace:System.Windows.Controls;assembly=PresentationFramework"
    
    Title="DataPage"
    d:DataContext="{d:DesignInstance local:DataPage,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">

    <Grid x:Name="dataGrid" Visibility="Visible">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 타이틀 -->
        <Grid Grid.Row="0">
            <ui:TextBlock HorizontalAlignment="Left" VerticalAlignment="Center" Height="50" FontWeight="Bold" FontSize="40" Margin="0,0,0,10">
                <Run Text="맞팔로우 정보" Foreground="White"/>
            </ui:TextBlock>
        </Grid>

        <!-- 페이지 메시지 표시 -->
        <Grid Grid.Row="1">
            <ui:TextBlock HorizontalAlignment="Left" VerticalAlignment="Center" Height="20" FontSize="15" Margin="10,20,0,100">
                <Run Text="test" Foreground="LightSlateGray" />
            </ui:TextBlock>
        </Grid>

        <!--맞팔 DataGrid -->
        <Grid Grid.Row="2">
            <ui:DataGrid x:Name="dgF4F" ItemsSource="{Binding ViewModel.F4f}" Width="auto" Height="400" VerticalAlignment="Top" HorizontalAlignment="Left" GridLinesVisibility="Horizontal" AutoGenerateColumns="False" Margin="0,0,0,10">
                <ui:DataGrid.Columns>
                    
                    <!-- 맞팔 아이디 -->
                    <dg:DataGridTextColumn Header="ID" Binding="{Binding Value}" Width="300"/>
    
                    <!-- 맞팔 인스타 주소 -->
                    <dg:DataGridTemplateColumn Header="주소" Width="450">
                        <dg:DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock>
                                    <Hyperlink NavigateUri="{Binding Href}" RequestNavigate="Hyperlink_RequestNavigate">
                                        <TextBlock Text="{Binding Href}" />
                                    </Hyperlink>    
                                </TextBlock>
                            </DataTemplate>
                        </dg:DataGridTemplateColumn.CellTemplate>
                    </dg:DataGridTemplateColumn>

                    <!-- 맞팔로우 기간 (일) -->
                    <dg:DataGridTextColumn Header="맞팔로우 기간" Binding="{Binding DateFromToday}" Width="250"/>
                </ui:DataGrid.Columns>
            </ui:DataGrid>
        </Grid>
        
        
        
    </Grid>
</Page>
